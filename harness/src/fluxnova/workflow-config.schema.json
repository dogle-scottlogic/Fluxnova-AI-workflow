{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "workflow-config.schema.json",
  "title": "Fluxnova Workflow Config",
  "description": "Configuration for deploying, running, and evaluating a Fluxnova BPMN workflow.",
  "type": "object",
  "required": ["fluxnova_url", "bpmn_path", "process_key", "deployment_name"],
  "additionalProperties": false,
  "properties": {
    "fluxnova_url": {
      "type": "string",
      "description": "Base URL of the Fluxnova engine REST API (e.g. http://localhost:8080/engine-rest)."
    },
    "bpmn_path": {
      "type": "string",
      "description": "Path to the BPMN file — relative to the repo root, or absolute."
    },
    "process_key": {
      "type": "string",
      "description": "The 'id' attribute on <process> in the BPMN."
    },
    "deployment_name": {
      "type": "string",
      "description": "Human-readable deployment name shown in Cockpit."
    },
    "subprocess_id": {
      "type": "string",
      "description": "BPMN element ID of the ad-hoc subprocess that contains the LLM agent."
    },
    "variables": {
      "type": "object",
      "description": "Initial process variables passed to the workflow instance.",
      "additionalProperties": true
    },
    "mock_workers": {
      "type": "object",
      "description": "Mock external-task workers. Keys are Camunda topic names; values are the output variables to complete each task with.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "user_tasks": {
      "type": "object",
      "description": "User task completions. Keys are task definition keys; values are variables to complete each task with.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "dataset_path": {
      "type": "string",
      "description": "Path to the golden dataset JSON file (relative to the repo root). Generate with datasets/<workflow>/generate.py."
    },
    "available_tools": {
      "type": "object",
      "description": "Tools available to the LLM agent. Keys are BPMN service-task element IDs; values are display names used in evaluation.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "expected_tools": {
      "type": "array",
      "description": "Ordered list of tool-expectation rules evaluated against the run's inputVariables at evaluation time. Rules use GitLab CI-style 'if' expressions.",
      "items": {
        "$ref": "#/$defs/ExpectedToolRule"
      }
    }
  },
  "$defs": {
    "ExpectedToolRule": {
      "type": "object",
      "description": "A single tool-expectation rule. 'tool' is always required; 'if' makes the rule conditional.",
      "required": ["tool"],
      "additionalProperties": false,
      "properties": {
        "tool": {
          "type": "string",
          "description": "Display name of the expected tool. Must match a value in available_tools."
        },
        "if": {
          "type": "string",
          "description": "Optional condition — '$variableName == value' or '$variableName != value'. Resolved against inputVariables from the agent-history report.",
          "pattern": "^\\$\\w+\\s*(==|!=)\\s*.+$"
        }
      }
    }
  }
}
